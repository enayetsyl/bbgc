const SectionHeadingText = ({ text }) => {
  return (
    <div className="text-center">
      <h4 className="text-3xl md:text-5xl font-bold">{text}</h4>
    </div>
  );
};

export default SectionHeadingText;
